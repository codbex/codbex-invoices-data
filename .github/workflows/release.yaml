name: Release - Module

on:
  workflow_dispatch:
    inputs:
      release-version:
        description: Release Version
        required: true
        default: 1.0.0

run-name: 'version set to ${{ inputs.release-version }} for release'

jobs:
  main:
    uses: codbex/codbex-infra/.github/workflows/module-release.yaml@main
    with:
      packages: codbex-invoices-data
      packages-build: codbex-invoices-data
      release-version: ${{ inputs.release-version }}
      release-content: |
        ## codbex-invoices-data - ${{ inputs.release-version }}
  
        Invoices Data Module
  
        ## Installation
  
        ```
        npm install @codbex/codbex-invoices-data@${{ inputs.release-version }}
        ```
  
    secrets: inherit